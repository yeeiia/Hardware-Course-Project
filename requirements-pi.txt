# Install PyTorch/torchvision for Raspberry Pi separately if needed.
# Then install these shared project dependencies inside /home/pi/.venv.
PyYAML
numpy
datasets
tqdm
scikit-learn
matplotlib
psutil
pytest
